<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<%= link_to 'Edit', edit_task_path(id: @task.id, project_id: @task.project.id) %> |
<%= link_to 'Back', tasks_path(project_id: @task.project.id) %>
